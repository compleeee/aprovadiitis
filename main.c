#include <stdio.h>
#include <stdlib.h>

double main()
{
    double a,b;
    printf("inserisci i valori");
    scanf("%lf %lf",&a &b);
    double area = a*b;
    printf("area = %2.lf",area);

    return 0;

}
